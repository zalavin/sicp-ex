#lang sicp

;; An if statement for both applicative and normal order functions will execute only hitted branch.
;; An ordinary function (that is new-if) evaluates all parameters along with an infinite calls to sqrt-iter function.